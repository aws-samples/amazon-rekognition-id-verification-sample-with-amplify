import { DashboardProps } from "../common/dashboard-props";


export const UpdateUserPhoto = (dashProps: DashboardProps) => {
    return (
        <div>
        </div>
    );
}

export default UpdateUserPhoto;